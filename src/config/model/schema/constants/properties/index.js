import ContactProperties from './contact';
import ScheduleProperties from './schedule';
import InstitutionProperties from './institution';
export const Contact = ContactProperties;
export const Schedule = ScheduleProperties;
export const Institution = InstitutionProperties;
export default {ContactProperties, InstitutionProperties, ScheduleProperties};